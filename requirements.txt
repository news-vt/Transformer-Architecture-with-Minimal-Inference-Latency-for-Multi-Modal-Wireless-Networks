# Install with:  pip install -r requirements.txt
#
# torch / torchvision are pinned to their CUDA 12.1 wheels via the extra index
# below. If your CUDA toolkit differs, change the URL (e.g. cu118) or the versions.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.1.2
torchvision==0.16.2

numpy==1.26.4
pandas==2.2.3
pillow==10.4.0
open3d==0.19.0
wandb==0.17.4
